# Data

Run:

```bash
python download_data.py
```

The script downloads MovieLens 1M from GroupLens and converts the ratings and movie metadata into CSV files.

Generated files:

- `ratings.csv`
- `movies.csv`

Do not commit the downloaded dataset to GitHub unless its current distribution terms permit it.
